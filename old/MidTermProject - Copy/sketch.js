var tableWidth, tableHeight, pocketSize;

function setup() {
  createCanvas(1000, 600);

  tableWidth = 800;
  tableHeight = tableWidth/2;
  pocketSize = (tableWidth/36)*1.5;

  drawTable();
}

function draw() {
  frameRate(60); // Set framerate limit
  background("lightyellow");

}

function drawTable(){
  
}